#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct books{
	char name[50];
	char author[20];
	char press[30];
	char year[10];
};

int main(int argc, char const *argv[]) {
	int op, i, j, n, clear = 0; //variable declaration
	int total_line = 0; //total line number
	char temp_str[200];
	char search_tmp[200];
	struct books info[500]; //books List

	return 0;
}
