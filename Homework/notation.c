#include <stdio.h>
#include <string.h>
#define MAX 100

int main(int argc, char  *argv[]) {

	char number[MAX];
	int i = 0, len;

	scanf("%c",number[i] );

	//len = strlen(number);
	//}
	//for(i = 0; i <)
	//printf("%d" ,len);
	for(i = 0; i < len; i++)
	{
			printf("%c",number[len] );

	}
	return 0;
}
